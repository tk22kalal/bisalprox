diff --git a/biisal/server/stream_routes.py b/biisal/server/stream_routes.py
index 2c812f6..9eac313 100644
--- a/biisal/server/stream_routes.py
+++ b/biisal/server/stream_routes.py
@@ -52,6 +52,7 @@ async def render_prepare_page(temp_data):
     template_content = template_content.replace("{{file_size}}", file_size)
     template_content = template_content.replace("{{token}}", temp_data['token'])
     template_content = template_content.replace("{{tag}}", tag)
+    template_content = template_content.replace("{{prepare_timeout_ms}}", str(Var.PREPARE_REQUEST_TIMEOUT_MS))
 
     return template_content
 
@@ -431,7 +432,7 @@ async def media_streamer(request: web.Request, id: int, secure_hash: str):
         try:
             file_id = await asyncio.wait_for(
                 streamer.get_file_properties(id),
-                timeout=15
+                timeout=Var.FILE_PROPERTIES_TIMEOUT
             )
             index = candidate_index
             tg_connect = streamer
diff --git a/biisal/template/prepare.html b/biisal/template/prepare.html
index 0ad0fbe..84a0f51 100644
--- a/biisal/template/prepare.html
+++ b/biisal/template/prepare.html
@@ -188,6 +188,9 @@
     </div>
 
     <script>
+        // Abort timeout for generate/download fetches (ms). Injected by the
+        // backend from Var.PREPARE_REQUEST_TIMEOUT_MS; falls back to 90000.
+        const PREPARE_TIMEOUT_MS = parseInt("{{prepare_timeout_ms}}", 10) || 90000;
         const urlParams = new URLSearchParams(window.location.search);
         const linkType  = urlParams.get('type');
 
@@ -211,7 +214,7 @@
 
             try {
                 const controller = new AbortController();
-                const tid = setTimeout(() => controller.abort(), 45000);
+                const tid = setTimeout(() => controller.abort(), PREPARE_TIMEOUT_MS);
                 const response = await fetch(`/api/generate/{{token}}?player=${playerType}`, {
                     signal: controller.signal,
                     headers: { 'Accept': 'application/json' }
@@ -261,7 +264,7 @@
 
             try {
                 const controller = new AbortController();
-                const tid = setTimeout(() => controller.abort(), 45000);
+                const tid = setTimeout(() => controller.abort(), PREPARE_TIMEOUT_MS);
                 const response = await fetch('/api/download/{{token}}', {
                     signal: controller.signal,
                     headers: { 'Accept': 'application/json' }
diff --git a/biisal/utils/custom_dl.py b/biisal/utils/custom_dl.py
index d9cc593..8f1d972 100644
--- a/biisal/utils/custom_dl.py
+++ b/biisal/utils/custom_dl.py
@@ -201,22 +201,47 @@ class ByteStreamer:
                 location = await self.get_location(file_id)
                 
                 for _ in range(part_count):
-                    try:
-                        r = await media_session.send(
-                            raw.functions.upload.GetFile(
-                                location=location, offset=current_offset, limit=chunk_size
-                            ),
-                        )
-                        if isinstance(r, raw.types.upload.File):
-                            await queue.put(r.bytes)
-                        else:
-                            await queue.put(None) # Signal error/end
+                    r = None
+                    last_exc = None
+                    # Retry each chunk on transient timeout/network errors
+                    # instead of silently returning None on the first failure.
+                    for retry in range(Var.TG_CHUNK_RETRY + 1):
+                        try:
+                            r = await media_session.send(
+                                raw.functions.upload.GetFile(
+                                    location=location, offset=current_offset, limit=chunk_size
+                                ),
+                            )
+                            last_exc = None
+                            break
+                        except (asyncio.TimeoutError, TimeoutError, ConnectionError, OSError) as e:
+                            last_exc = e
+                            logging.warning(
+                                f"Chunk fetch timeout/network error (client {index}, "
+                                f"offset {current_offset}, retry {retry + 1}/{Var.TG_CHUNK_RETRY}): {e}"
+                            )
+                            if retry < Var.TG_CHUNK_RETRY:
+                                await asyncio.sleep(Var.TG_CHUNK_BACKOFF * (retry + 1))
+                        except Exception as e:
+                            last_exc = e
+                            logging.error(
+                                f"Producer non-retryable error (client {index}, "
+                                f"offset {current_offset}): {e}"
+                            )
                             break
-                        current_offset += chunk_size
-                    except Exception as e:
-                        logging.error(f"Producer error in client {index}: {e}")
+                    if r is None or last_exc is not None:
+                        logging.error(
+                            f"Giving up chunk (client {index}, offset {current_offset}) "
+                            f"after {Var.TG_CHUNK_RETRY} retries. Last error: {last_exc}"
+                        )
                         await queue.put(None)
                         break
+                    if isinstance(r, raw.types.upload.File):
+                        await queue.put(r.bytes)
+                    else:
+                        await queue.put(None) # Signal error/end
+                        break
+                    current_offset += chunk_size
                 # End signal
                 await queue.put(None)
             except Exception as e:
diff --git a/biisal/utils/file_properties.py b/biisal/utils/file_properties.py
index 8d3414d..2304eff 100644
--- a/biisal/utils/file_properties.py
+++ b/biisal/utils/file_properties.py
@@ -18,15 +18,20 @@ async def parse_file_unique_id(message: "Messages") -> Optional[str]:
 
 async def get_file_ids(client: Client, chat_id: int, id: int) -> Optional[FileId]:
     message = await client.get_messages(chat_id, id)
-    if message.empty:
+    # Defensive guards: bail out cleanly instead of cascading into NoneType errors.
+    if message is None or message.empty:
         raise FIleNotFound
     media = get_media_from_message(message)
-    file_unique_id = await parse_file_unique_id(message)
+    if media is None:
+        raise FIleNotFound
     file_id = await parse_file_id(message)
+    if file_id is None:
+        raise FIleNotFound
+    file_unique_id = await parse_file_unique_id(message)
     setattr(file_id, "file_size", getattr(media, "file_size", 0))
     setattr(file_id, "mime_type", getattr(media, "mime_type", ""))
     setattr(file_id, "file_name", getattr(media, "file_name", ""))
-    setattr(file_id, "unique_id", file_unique_id)
+    setattr(file_id, "unique_id", file_unique_id or "")
     setattr(file_id, "has_thumb", bool(getattr(media, "thumbs", None)))
     return file_id
 
diff --git a/biisal/vars.py b/biisal/vars.py
index 2ae33cf..bde1965 100644
--- a/biisal/vars.py
+++ b/biisal/vars.py
@@ -64,6 +64,19 @@ class Var(object):
     RECAPTCHA_SITE_KEY = str(getenv('RECAPTCHA_SITE_KEY', '6LdCK_crAAAAAD702QCUelFDiZPr5wqL-3qbgk2u'))
     RECAPTCHA_SECRET_KEY = str(getenv('RECAPTCHA_SECRET_KEY', '6LdCK_crAAAAAMiFPR9Pk5u3Zvnj6G8rNEORAsEV'))
 
+    # ── Resilience / timeout tuning (env-driven, safe defaults) ──────────────
+    # Seconds the app waits for Telegram to resolve file properties before
+    # trying the next client. Raised from the old hardcoded 15s.
+    FILE_PROPERTIES_TIMEOUT = int(getenv('FILE_PROPERTIES_TIMEOUT', '60'))
+    # How many extra times to retry a single Telegram chunk (GetFile) on a
+    # transient timeout/network error before giving up on that chunk.
+    TG_CHUNK_RETRY = int(getenv('TG_CHUNK_RETRY', '3'))
+    # Base backoff (seconds) between chunk retries; scales linearly per retry.
+    TG_CHUNK_BACKOFF = int(getenv('TG_CHUNK_BACKOFF', '2'))
+    # Frontend (prepare.html) fetch abort timeout in milliseconds. Injected
+    # into the template so slow-but-working backends are not aborted early.
+    PREPARE_REQUEST_TIMEOUT_MS = int(getenv('PREPARE_REQUEST_TIMEOUT_MS', '90000'))
+
     @classmethod
     def get_fqdn(cls):
         if cls.SERVE_DOMAIN == 'web':
